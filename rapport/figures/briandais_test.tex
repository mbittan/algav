% Red-black tree
% Author: Madit
\documentclass{article}
\usepackage{tikz}
%%%<
\usepackage{verbatim}
\usepackage[active,tightpage]{preview}
\PreviewEnvironment{tikzpicture}
\setlength{\PreviewBorder}{10pt}%
%%%>
\usetikzlibrary{arrows}
\usetikzlibrary{shapes}
\usetikzlibrary{calc,positioning}

\tikzset{
  treenode/.style = {align=center, inner sep=0pt, text centered,
    font=\sffamily},
  arn_n/.style = {treenode, circle, white, font=\sffamily\bfseries, draw=black,
    fill=black, text width=1.5em},% arbre rouge noir, noeud noir
  arn_r/.style = {treenode, circle, red, draw=red, 
    text width=1.5em, very thick},% arbre rouge noir, noeud rouge
  arn_x/.style = {treenode, rectangle, draw=black,
    minimum width=0.5em, minimum height=0.5em},% arbre rouge noir, nil
  racine/.style = {rectangle split, rectangle split horizontal, rounded corners=5pt},
  fils/.style   = {rectangle split, rectangle split horizontal, rounded corners=5pt, anchor=north,
                   edge from parent path={(\tikzparentnode) |- (\tikzchildnode.north)},
                   rectangle split parts=3, 
                   rectangle split draw splits=false},
  frere/.style   = {rectangle split, rectangle split horizontal, rounded corners=5pt, anchor=west,
                   edge from parent path={(\tikzparentnode) -- (\tikzchildnode.west)},
                   rectangle split parts=3, 
                   rectangle split draw splits=false}
}

\begin{document}
\begin{tikzpicture}[->]
%[->,>=stealth',level/.style={sibling distance = 5cm/#1,
%      level distance = 1.5cm}]
%  \node[name=data, rectangle split, 
%    rectangle split horizontal, 
%    rectangle split parts=3, 
%    rectangle split draw splits=false, 
%    draw, rounded corners=5pt] (main) 
%       {A \nodepart{two} . 
%         \nodepart{three} $\emptyset$};

%       \node[name=data, rectangle split, 
%         rectangle split horizontal, 
%        rectangle split parts=3, 
%         rectangle split draw splits=false, 
%         draw, rounded corners=5pt] (main) 
%            {B \nodepart{two} . 
%              \nodepart{three} $\emptyset$};

  
  \node[racine]{A \nodepart{two} . 
                  \nodepart{three} .}
  child{ node[fils]{B \nodepart{two} .
                      \nodepart{three} $\emptyset$}}
  child{ node[frere]{R \nodepart{two} .
                       \nodepart{three} $\emptyset$}};
  

\end{tikzpicture}
\end{document}
